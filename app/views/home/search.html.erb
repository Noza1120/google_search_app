<%= render 'layouts/form' %>
<h2>検索結果</h2>
<ul class="results">
  <% @items.each do |item| %>
    <li>
      <p class="lead">
        <%= item[:title] %>
      </p>
      <a class="text-muted" target="_blank">
        <%= item[:link] %>
      </a>
    </li>
  <% end %>
</ul>